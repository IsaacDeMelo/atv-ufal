<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes da Atividade</title>
    <link rel="stylesheet" href="/tailwind.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
      body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-50 min-h-screen flex items-center justify-center p-4">
  <div class="w-full max-w-lg bg-white rounded-2xl shadow-xl p-8 md:p-10 border border-white/20 backdrop-blur-sm">
    <div class="flex flex-col items-center mb-8">
      <span class="inline-flex items-center gap-2 mb-3">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke="currentColor"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" fill="none"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 16v-4m0-4h.01" /></svg>
        <span class="text-indigo-600 font-semibold tracking-wide">Administração</span>
      </span>
      <h1 class="text-3xl font-bold text-center text-gray-900 tracking-tight bg-gradient-to-r from-indigo-600 to-blue-600 bg-clip-text text-transparent">Detalhes da Atividade</h1>
    </div>
    <div class="space-y-4">
      <div>
        <span class="block text-xs text-gray-500">Matéria</span>
        <span class="block text-xl font-semibold text-gray-800"><%= atividade.materia %></span>
      </div>
      <% if (atividade.dataEntrega) { %>
      <div>
        <span class="block text-xs text-gray-500">Data de Entrega</span>
        <span class="block text-base text-gray-700"><%= atividade.dataEntrega %></span>
      </div>
      <% } %>
      <div>
        <span class="block text-xs text-gray-500">Descrição</span>
        <span class="block text-base text-gray-700 whitespace-pre-line"><%= atividade.descricao %></span>
      </div>
      <% if (atividade.nota !== null && atividade.nota !== undefined) { %>
      <div>
        <span class="block text-xs text-gray-500">Nota</span>
        <span class="inline-block bg-indigo-100 text-indigo-800 px-3 py-1 rounded-full text-sm font-medium"><%= atividade.nota %></span>
      </div>
      <% } %>
    </div>
    <div class="flex justify-end mt-8">
      <a href="/" class="px-5 py-2.5 bg-gradient-to-r from-indigo-600 to-blue-600 text-white rounded-lg shadow hover:shadow-md hover:from-indigo-700 hover:to-blue-700 transition-all font-semibold">Voltar</a>
    </div>
  </div>
</body>
</html>
