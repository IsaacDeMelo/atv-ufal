<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Atividades</title>
    <link rel="stylesheet" href="/tailwind.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
      body {
        font-family: 'Inter', sans-serif;
      }
      .activity-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
      }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-50 min-h-screen flex items-center justify-center p-4">
  <div class="w-full max-w-2xl bg-white rounded-2xl shadow-xl p-8 md:p-10 border border-white/20 backdrop-blur-sm">
    <div class="flex flex-col items-center mb-8">
      <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
        <span class="inline-flex items-center gap-2 mb-3">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke="currentColor"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" fill="none"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 16v-4m0-4h.01" /></svg>
          <span class="text-indigo-600 font-semibold tracking-wide">Administração</span>
        </span>
      <% } %>
      <h1 class="text-4xl font-bold text-center text-gray-900 tracking-tight bg-gradient-to-r from-indigo-600 to-blue-600 bg-clip-text text-transparent">Atividades</h1>
      <p class="text-gray-500 text-center mt-3 max-w-md mx-auto">Acompanhe e gerencie suas atividades acadêmicas de forma simples e eficiente.</p>
    </div>
    <div class="flex justify-center mb-8 gap-4">
      <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
        <a href="/new" class="px-5 py-2.5 bg-gradient-to-r from-indigo-600 to-blue-600 text-white rounded-lg shadow hover:shadow-md hover:from-indigo-700 hover:to-blue-700 transition-all font-semibold flex items-center gap-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
          </svg>
          Nova Atividade
        </a>
        <form action="/logout" method="POST">
          <button type="submit" class="ml-2 px-4 py-2 text-sm text-gray-500 bg-gray-100 rounded-lg border border-gray-200 hover:bg-gray-200 transition">Sair</button>
        </form>
      <% } else { %>
        <a href="/admin" class="px-4 py-2 text-sm text-gray-500 bg-gray-100 rounded-lg border border-gray-200 hover:bg-gray-200 transition">Entrar como administrador</a>
      <% } %>
    </div>
    <ul class="space-y-5">
      <% if (atividades.length === 0) { %>
        <li class="text-center text-neutral-400">Nenhuma atividade cadastrada.</li>
      <% } %>
      <% atividades.forEach(atividade => { %>
        <li class="activity-card bg-white border-2 border-indigo-200 rounded-xl shadow-sm p-6 flex flex-col items-center transition-all duration-200 hover:border-indigo-400">
          <a href="/atividades/<%= atividade.id %>" class="w-full flex flex-col items-center group">
            <span class="block text-xl font-semibold text-gray-800 mb-1 group-hover:text-indigo-700 transition"> <%= atividade.materia %> </span>
            <% if (atividade.dataEntrega) { %>
              <span class="block text-xs text-gray-400 mb-2">Entrega: <%= atividade.dataEntrega %></span>
            <% } %>
          </a>
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>
